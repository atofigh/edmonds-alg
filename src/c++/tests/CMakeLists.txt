

GET_TARGET_PROPERTY(TEST_ALGORITHM_EXE test-algorithm LOCATION)

FILE(GLOB tests RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} ${CMAKE_CURRENT_SOURCE_DIR}/*xml)
FOREACH(i ${tests})
ADD_TEST(${i} ${TEST_ALGORITHM_EXE} ${CMAKE_CURRENT_SOURCE_DIR}/${i})
ENDFOREACH(i)

